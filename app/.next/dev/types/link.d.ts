// This file is generated automatically by Next.js
// Do not edit this file manually

// Type definitions for Next.js routes

/**
 * Internal types used by the Next.js router and Link component.
 * These types are not meant to be used directly.
 * @internal
 */
declare namespace __next_route_internal_types__ {
  type SearchOrHash = `?${string}` | `#${string}`
  type WithProtocol = `${string}:${string}`

  type Suffix = '' | SearchOrHash

  type SafeSlug<S extends string> = S extends `${string}/${string}`
    ? never
    : S extends `${string}${SearchOrHash}`
    ? never
    : S extends ''
    ? never
    : S

  type CatchAllSlug<S extends string> = S extends `${string}${SearchOrHash}`
    ? never
    : S extends ''
    ? never
    : S

  type OptionalCatchAllSlug<S extends string> =
    S extends `${string}${SearchOrHash}` ? never : S

  type StaticRoutes = 
    | `/`
    | `/api/ai/match-file`
    | `/api/ai/troubleshoot`
    | `/api/cluster-health`
    | `/api/cluster/health`
    | `/api/configmaps`
    | `/api/contexts`
    | `/api/cronjobs`
    | `/api/daemonsets`
    | `/api/dashboard/summary`
    | `/api/deployments`
    | `/api/events`
    | `/api/github-app/callback`
    | `/api/github-app/installations`
    | `/api/github-app/logout`
    | `/api/github-app/repositories`
    | `/api/github/analyze-structure`
    | `/api/github/auth-status`
    | `/api/github/branches`
    | `/api/github/create-multi-file-pr`
    | `/api/github/create-pr`
    | `/api/github/file`
    | `/api/github/files`
    | `/api/github/kustomize`
    | `/api/github/match-file`
    | `/api/github/merge-pr`
    | `/api/github/repos`
    | `/api/github/tree`
    | `/api/hpa`
    | `/api/ingress`
    | `/api/jobs`
    | `/api/labels`
    | `/api/limitranges`
    | `/api/metrics/pods`
    | `/api/namespaces`
    | `/api/nodes`
    | `/api/pods`
    | `/api/pvcs`
    | `/api/pvs`
    | `/api/resourcequotas`
    | `/api/secrets`
    | `/api/services`
    | `/api/statefulsets`
    | `/api/stream`
    | `/api/test-connection`
    | `/badge-demo`
    | `/components-showcase`
    | `/configmaps`
    | `/cronjobs`
    | `/daemonsets`
    | `/demo`
    | `/demo/cronjobs`
    | `/demo/daemonsets`
    | `/demo/deployments`
    | `/demo/jobs`
    | `/demo/namespaces`
    | `/demo/nodes`
    | `/demo/pods`
    | `/demo/repo-browser`
    | `/demo/services`
    | `/demo/statefulsets`
    | `/deployments`
    | `/events`
    | `/hpa`
    | `/ingress`
    | `/jobs`
    | `/labels`
    | `/namespaces`
    | `/nodes`
    | `/pods`
    | `/pv`
    | `/repo-browser`
    | `/secrets`
    | `/services`
    | `/settings`
    | `/statefulsets`
    | `/topology`
  type DynamicRoutes<T extends string = string> = 
    | `/api/auth/${CatchAllSlug<T>}`
    | `/api/configmaps/${SafeSlug<T>}`
    | `/api/configmaps/${SafeSlug<T>}/events`
    | `/api/cronjobs/${SafeSlug<T>}`
    | `/api/daemonsets/${SafeSlug<T>}`
    | `/api/deployments/${SafeSlug<T>}`
    | `/api/deployments/${SafeSlug<T>}/events`
    | `/api/deployments/${SafeSlug<T>}/pods`
    | `/api/deployments/${SafeSlug<T>}/restart`
    | `/api/ingress/${SafeSlug<T>}`
    | `/api/jobs/${SafeSlug<T>}`
    | `/api/namespaces/${SafeSlug<T>}`
    | `/api/nodes/${SafeSlug<T>}`
    | `/api/nodes/${SafeSlug<T>}/events`
    | `/api/nodes/${SafeSlug<T>}/pods`
    | `/api/pods/${SafeSlug<T>}`
    | `/api/pods/${SafeSlug<T>}/events`
    | `/api/pods/${SafeSlug<T>}/logs`
    | `/api/pods/${SafeSlug<T>}/restart`
    | `/api/resources/${SafeSlug<T>}/${SafeSlug<T>}/yaml`
    | `/api/secrets/${SafeSlug<T>}`
    | `/api/secrets/${SafeSlug<T>}/events`
    | `/api/services/${SafeSlug<T>}`
    | `/api/statefulsets/${SafeSlug<T>}`
    | `/configmaps/${SafeSlug<T>}`
    | `/cronjobs/${SafeSlug<T>}`
    | `/daemonsets/${SafeSlug<T>}`
    | `/demo/${CatchAllSlug<T>}`
    | `/demo/configmaps/${SafeSlug<T>}`
    | `/demo/cronjobs/${SafeSlug<T>}`
    | `/demo/daemonsets/${SafeSlug<T>}`
    | `/demo/deployments/${SafeSlug<T>}`
    | `/demo/ingress/${SafeSlug<T>}`
    | `/demo/jobs/${SafeSlug<T>}`
    | `/demo/namespaces/${SafeSlug<T>}`
    | `/demo/nodes/${SafeSlug<T>}`
    | `/demo/pods/${SafeSlug<T>}`
    | `/demo/secrets/${SafeSlug<T>}`
    | `/demo/services/${SafeSlug<T>}`
    | `/demo/statefulsets/${SafeSlug<T>}`
    | `/deployments/${SafeSlug<T>}`
    | `/ingress/${SafeSlug<T>}`
    | `/jobs/${SafeSlug<T>}`
    | `/namespaces/${SafeSlug<T>}`
    | `/nodes/${SafeSlug<T>}`
    | `/pods/${SafeSlug<T>}`
    | `/secrets/${SafeSlug<T>}`
    | `/services/${SafeSlug<T>}`
    | `/statefulsets/${SafeSlug<T>}`

  type RouteImpl<T> = 
    | StaticRoutes
    | SearchOrHash
    | WithProtocol
    | `${StaticRoutes}${SearchOrHash}`
    | (T extends `${DynamicRoutes<infer _>}${Suffix}` ? T : never)
    
}

declare module 'next' {
  export { default } from 'next/types.js'
  export * from 'next/types.js'

  export type Route<T extends string = string> =
    __next_route_internal_types__.RouteImpl<T>
}

declare module 'next/link' {
  export { useLinkStatus } from 'next/dist/client/link.js'

  import type { LinkProps as OriginalLinkProps } from 'next/dist/client/link.js'
  import type { AnchorHTMLAttributes, DetailedHTMLProps } from 'react'
  import type { UrlObject } from 'url'

  type LinkRestProps = Omit<
    Omit<
      DetailedHTMLProps<
        AnchorHTMLAttributes<HTMLAnchorElement>,
        HTMLAnchorElement
      >,
      keyof OriginalLinkProps
    > &
      OriginalLinkProps,
    'href'
  >

  export type LinkProps<RouteInferType> = LinkRestProps & {
    /**
     * The path or URL to navigate to. This is the only required prop. It can also be an object.
     * @see https://nextjs.org/docs/api-reference/next/link
     */
    href: __next_route_internal_types__.RouteImpl<RouteInferType> | UrlObject
  }

  export default function Link<RouteType>(props: LinkProps<RouteType>): JSX.Element
}

declare module 'next/navigation' {
  export * from 'next/dist/client/components/navigation.js'

  import type { NavigateOptions, AppRouterInstance as OriginalAppRouterInstance } from 'next/dist/shared/lib/app-router-context.shared-runtime.js'
  import type { RedirectType } from 'next/dist/client/components/redirect-error.js'
  
  interface AppRouterInstance extends OriginalAppRouterInstance {
    /**
     * Navigate to the provided href.
     * Pushes a new history entry.
     */
    push<RouteType>(href: __next_route_internal_types__.RouteImpl<RouteType>, options?: NavigateOptions): void
    /**
     * Navigate to the provided href.
     * Replaces the current history entry.
     */
    replace<RouteType>(href: __next_route_internal_types__.RouteImpl<RouteType>, options?: NavigateOptions): void
    /**
     * Prefetch the provided href.
     */
    prefetch<RouteType>(href: __next_route_internal_types__.RouteImpl<RouteType>): void
  }

  export function useRouter(): AppRouterInstance;
  
  /**
   * This function allows you to redirect the user to another URL. It can be used in
   * [Server Components](https://nextjs.org/docs/app/building-your-application/rendering/server-components),
   * [Route Handlers](https://nextjs.org/docs/app/building-your-application/routing/route-handlers), and
   * [Server Actions](https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations).
   *
   * - In a Server Component, this will insert a meta tag to redirect the user to the target page.
   * - In a Route Handler or Server Action, it will serve a 307/303 to the caller.
   * - In a Server Action, type defaults to 'push' and 'replace' elsewhere.
   *
   * Read more: [Next.js Docs: redirect](https://nextjs.org/docs/app/api-reference/functions/redirect)
   */
  export function redirect<RouteType>(
    /** The URL to redirect to */
    url: __next_route_internal_types__.RouteImpl<RouteType>,
    type?: RedirectType
  ): never;
  
  /**
   * This function allows you to redirect the user to another URL. It can be used in
   * [Server Components](https://nextjs.org/docs/app/building-your-application/rendering/server-components),
   * [Route Handlers](https://nextjs.org/docs/app/building-your-application/routing/route-handlers), and
   * [Server Actions](https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations).
   *
   * - In a Server Component, this will insert a meta tag to redirect the user to the target page.
   * - In a Route Handler or Server Action, it will serve a 308/303 to the caller.
   *
   * Read more: [Next.js Docs: redirect](https://nextjs.org/docs/app/api-reference/functions/redirect)
   */
  export function permanentRedirect<RouteType>(
    /** The URL to redirect to */
    url: __next_route_internal_types__.RouteImpl<RouteType>,
    type?: RedirectType
  ): never;
}

declare module 'next/form' {
  import type { FormProps as OriginalFormProps } from 'next/dist/client/form.js'

  type FormRestProps = Omit<OriginalFormProps, 'action'>

  export type FormProps<RouteInferType> = {
    /**
     * `action` can be either a `string` or a function.
     * - If `action` is a string, it will be interpreted as a path or URL to navigate to when the form is submitted.
     *   The path will be prefetched when the form becomes visible.
     * - If `action` is a function, it will be called when the form is submitted. See the [React docs](https://react.dev/reference/react-dom/components/form#props) for more.
     */
    action: __next_route_internal_types__.RouteImpl<RouteInferType> | ((formData: FormData) => void)
  } & FormRestProps

  export default function Form<RouteType>(props: FormProps<RouteType>): JSX.Element
}
