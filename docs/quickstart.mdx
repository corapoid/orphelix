---
title: 'Quickstart'
description: 'Get started with Orphelix in under 5 minutes'
---

## Prerequisites

Before you begin, make sure you have:

<AccordionGroup>
  <Accordion icon="node-js" title="Node.js 24 or higher">
    Download from [nodejs.org](https://nodejs.org)
  </Accordion>
  <Accordion icon="terminal" title="kubectl (optional)">
    Only needed for connecting to real clusters. Install from [kubernetes.io](https://kubernetes.io/docs/tasks/tools/)
  </Accordion>
</AccordionGroup>

## Installation

<Tabs>
  <Tab title="CLI (Recommended)">
    **Easiest way to run Orphelix - one command setup!**

    <Steps>
      <Step title="Clone the repository">
        ```bash
        git clone https://github.com/corapoid/orphelix.git
        cd orphelix/app
        ```
      </Step>

      <Step title="Start Orphelix">
        ```bash
        npm run orphelix start
        ```

        First run automatically:
        - Installs dependencies
        - Builds application
        - Configures custom hostname
        - Initializes database
        - Starts in background
      </Step>

      <Step title="Open in browser">
        ```bash
        npm run orphelix open
        ```

        Or visit: [http://orphelix.local:3000](http://orphelix.local:3000)
      </Step>
    </Steps>

    <Note>
      Orphelix runs as a background service using PM2. No need to keep terminal open!
    </Note>

    See [CLI Guide](/cli/overview) for all commands.
  </Tab>

  <Tab title="Development Mode">
    **For development and testing:**

    <Steps>
      <Step title="Clone the repository">
        ```bash
        git clone https://github.com/corapoid/orphelix.git
        cd orphelix/app
        ```
      </Step>

      <Step title="Install dependencies">
        ```bash
        npm install
        ```
      </Step>

      <Step title="Start the development server">
        ```bash
        npm run dev
        ```
      </Step>

      <Step title="Open in browser">
        Navigate to [http://localhost:3000](http://localhost:3000)

        The application starts in **Demo Mode** with realistic test data.
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Exploring Demo Mode

Demo Mode lets you explore all features without connecting to a real cluster:

<CardGroup cols={2}>
  <Card title="Dashboard" icon="chart-simple">
    View summary cards showing resource counts (Deployments, Pods, Nodes, etc.)
  </Card>
  <Card title="Deployments" icon="layer-group">
    Browse deployment lists, view details, and see associated pods
  </Card>
  <Card title="Pods" icon="cube">
    Inspect pod details, view container logs, and check events
  </Card>
  <Card title="Topology" icon="diagram-project">
    Visualize resource relationships in an interactive graph
  </Card>
</CardGroup>

<Note>
  Demo Mode uses realistic mock data that simulates a production environment. Perfect for testing, presentations, or learning Kubernetes concepts.
</Note>

## Connecting to a Real Cluster

<Steps>
  <Step title="Click the 'DEMO MODE' badge">
    Located in the top-right corner of the header
  </Step>

  <Step title="Select 'Real Cluster'">
    Choose your kubectl context from the dropdown
  </Step>

  <Step title="Select namespace">
    Pick the namespace you want to monitor (default: 'default')
  </Step>

  <Step title="Test connection">
    Click 'Test Connection' to verify cluster access
  </Step>

  <Step title="Switch mode">
    Click 'Switch to Real Cluster' to start monitoring live resources
  </Step>
</Steps>

<Warning>
  Make sure your `~/.kube/config` file is properly configured and you have appropriate permissions to access the cluster.
</Warning>

## Next Steps

<CardGroup cols={2}>
  <Card
    title="CLI Guide"
    icon="terminal"
    href="/cli/overview"
  >
    Learn all CLI commands and features
  </Card>
  <Card
    title="User Guide"
    icon="book"
    href="/user/dashboard"
  >
    Learn how to use all features in detail
  </Card>
  <Card
    title="GitHub Integration"
    icon="github"
    href="/user/github/overview"
  >
    Set up GitOps workflow with YAML editor and PR creation
  </Card>
  <Card
    title="Configuration"
    icon="gear"
    href="/user/configuration/cluster-connection"
  >
    Configure cluster connections, namespaces, and settings
  </Card>
</CardGroup>

## CLI Management

Orphelix CLI provides powerful commands for managing your dashboard:

```bash
# View status
npm run orphelix status

# View logs
npm run orphelix logs

# Restart application
npm run orphelix restart

# Create backup
npm run orphelix backup

# Enable auto-startup on boot
npm run orphelix startup

# Run health checks
npm run orphelix doctor
```

See [CLI Commands](/cli/commands/start) for complete reference.
