# ============================================================================
# NextAuth - GitHub OAuth Authentication
# ============================================================================
# For user authentication and session management
#
# Setup:
# 1. Go to GitHub Settings -> Developer settings -> OAuth Apps
# 2. Click "New OAuth App"
# 3. Fill in:
#    - Application name: Orphelix
#    - Homepage URL: http://localhost:3000
#    - Authorization callback URL: http://localhost:3000/api/auth/callback/github
# 4. Copy Client ID and generate Client Secret
# 5. Generate NEXTAUTH_SECRET: openssl rand -base64 32
#
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32
GITHUB_ID=your-github-oauth-client-id
GITHUB_SECRET=your-github-oauth-client-secret

# ============================================================================
# GitHub App Integration (for repo management features)
# ============================================================================
# Granular permissions - users can select specific repositories to grant access to
#
# Setup: https://docs.github.com/en/apps/creating-github-apps
# 1. Go to GitHub Settings -> Developer settings -> GitHub Apps
# 2. Click "New GitHub App"
# 3. Fill in:
#    - GitHub App name: KubeVista
#    - Homepage URL: http://localhost:3000
#    - Callback URL: http://localhost:3000/api/github-app/callback
#    - Webhook: Disable (uncheck "Active")
#    - Repository permissions:
#      * Contents: Read & write
#      * Pull requests: Read & write
#      * Metadata: Read-only
#    - User permissions:
#      * Email addresses: Read-only
# 4. Generate a private key (download .pem file)
# 5. Copy App ID, Client ID, Client Secret
# 6. Convert private key: cat downloaded-key.pem | tr '\n' '|' (replace newlines with |)
#
GITHUB_APP_ID=123456
GITHUB_APP_SLUG=kubevista
GITHUB_APP_CLIENT_ID=Iv1.abc123
GITHUB_APP_CLIENT_SECRET=ghs_abc123xyz
GITHUB_APP_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----
NEXT_PUBLIC_GITHUB_APP_CLIENT_ID=Iv1.abc123
NEXT_PUBLIC_GITHUB_APP_SLUG=kubevista


# ============================================================================
